zchase/phpApache:

  Env:
    - PORT: "8006"

  PkgInclude:
    - simpleapp.php

  PreInstall:
    - echo "Beginning PreInstall"
    - sudo apt-get update
    - sudo apt-get install -y php5-cli

  Exec:
    - 'php -S 0.0.0.0:$PORT simpleapp.php'

