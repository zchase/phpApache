zchase/phpApache:

  PkgInclude:
    - simpleapp.php

  PreInstall:
    - echo "Beginning PreInstall"
    - sudo apt-get update
    - sudo apt-get install php5 libapache2-mod-php5

  Exec:
    - sudo service apache2 restart 

